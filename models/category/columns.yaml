# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: nethavn.gallery::lang.modeldata.name
        searchable: true

    created_at:
        label: nethavn.gallery::lang.modeldata.created

    updated_at:
        label: nethavn.gallery::lang.modeldata.updated
    
    galleries_count:
        label: Galleries
